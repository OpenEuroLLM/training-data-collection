name: FineMath 0.0.0
catalogue: finemath/0.0.0/data
suffix: .jsonl.zst
id: uuid
text: text
#
release:
  default:
    input: uuid
    pack: tree
#
# ./etc/plan.py --pattern finemath-0.0.0 --format yaml --budget datamix.txt
#
  finemath-3plus:
    sample: full
    shard: 
#
nemo-curator:
  default:
    input: uuid
#
uuid:
#
# for i in finemath-0.0.0/source/finemath-3plus/*.jsonl.zst; do
#   echo $i;
#   /appl/local/openeurollm/training/catalogue/etc/zstdcat.py \
#     --mode json --uuid $i \
#   | zstd -z -o finemath-0.0.0/uuid/${i##finemath*/source/};
# done
#
  default:
    input: source
#
source:
  finemath-3plus:
