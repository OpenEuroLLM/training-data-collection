name: HPLT 4.0
catalogue: hplt/4.0
suffix: .jsonl.zst
text: text
id: id
#
release:
  default:
    input: source
    pack: tree
    scrub: [xml, md]
    sample: full
#
# ./etc/plan.py --pattern hplt --format yaml \
#   --budget datamix.txt | egrep -v 'sample|budget'
#
  clean/fao_Latn:
    shard: 99md
#
source:
  default:
    annotations: [doc_scores, web-register]
  clean/fao_Latn:
