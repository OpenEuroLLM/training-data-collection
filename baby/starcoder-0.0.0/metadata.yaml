name: StarCoder Training Dataset 0.0.0
huggingface: bigcode/starcoderdata
catalogue: starcoder/0.0.0/data
suffix: .jsonl.zst
text: content
release:
  default:
    input: source
    pack: flat
    shard: 100bd
#
# ./etc/plan.py --pattern starcoder-0.0.0 --format yaml \
#   --budget datamix.txt >> starcoder-0.0.0/metadata.yaml
#
  starcoder-0.0.0/megatron-lm:
    sample: full
